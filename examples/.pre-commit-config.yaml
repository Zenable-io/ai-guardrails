# Example pre-commit configuration using Zenable guardrails
# Copy this to your project root as .pre-commit-config.yaml

repos:
  - repo: https://github.com/Zenable-io/ai-guardrails
    rev: v1.0.0  # Update to latest version
    hooks:
      - id: zenable-check
